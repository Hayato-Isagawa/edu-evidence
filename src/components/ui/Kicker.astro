---
/**
 * セクションの上に置く mono 大文字ラベル。ADR 0009 が FV の kicker として
 * `font-mono text-xs uppercase tracking-widest text-accent` を決定値にしている。
 */
interface Props {
  tone?: "accent" | "sub";
  size?: "xs" | "2xs";
  /**
   * 出力要素。インライン文脈やフレックス子要素として置く箇所があり、
   * そこで <p> を出すと不正な入れ子になる。
   */
  as?: "p" | "span" | "div";
  class?: string;
}

const {
  tone = "accent",
  size = "xs",
  as: Tag = "p",
  class: className,
} = Astro.props;

const sizes = { xs: "text-xs", "2xs": "text-[10px]" } as const;
const tones = { accent: "text-accent", sub: "text-sub" } as const;
---

<Tag
  class:list={[
    "font-mono uppercase tracking-widest",
    sizes[size],
    tones[tone],
    className,
  ]}
>
  <slot />
</Tag>
